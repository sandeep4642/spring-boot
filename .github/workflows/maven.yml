name: CI

on:
  workflow_dispatch:
    inputs:
      javaversion:
        default: "11"
        type: number
  
jobs:
  Extract_Branch:
    uses: sandeep4642/github_actions_library/blob/.github/workflows/extract_branch.yml@main
